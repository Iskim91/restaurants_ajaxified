<li data-dose-id="<%=dose.id%>">
  <%= dose.ingredient.name %>-
  <%= dose.amount %>
  <%= link_to cocktail_dose_path(dose), method: :delete, remote: true,  data: { confirm: "Are you sure?" }  do%>
    <i class="far fa-trash-alt"></i>
  <% end %>
</li>
